# core — the backend domains (Mintlify pages)

Domain-level docs mirroring the `core/` tree: `agents.mdx` (the execution/dispatch domain),
`runtime.mdx` (the spawn/schedule kernel), `meetings.mdx` (capture + transcription).
