# contracts — published by `agent`

Language-neutral contracts this domain owns. Currently `workspace.v1` (the user-workspace template +
entity-frontmatter convention). Consumers reference these by path; `gate:schema` validates goldens ≡ schema.
