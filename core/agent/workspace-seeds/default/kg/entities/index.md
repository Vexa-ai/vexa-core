# entities

The knowledge graph's typed entities, one directory per type (`<type>/<slug>.md`). No entities yet — as you create a type directory, give it its own `index.md` and list new entities there.
