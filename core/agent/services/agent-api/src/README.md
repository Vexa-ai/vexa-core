# src — agent-api source root

Holds the `agent_api` Python package (the `pythonpath` for tests, per `pyproject.toml`). The package
is the front door; import from `agent_api`, never a deep module path (P6).
